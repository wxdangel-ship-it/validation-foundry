# tools

本目录用于存放仓库级迁移、QA 与验证工具。

当前阶段说明：

- `t02_liuzhijiao_export_gpx.py`
  - 用途：在六只脚已经停留于 `搜索 越野 -> 自驾车` 结果页时，批量打开详情并通过右上角 `...` 导出 `GPX`
  - 产物：`outputs/_tmp/t02_liuzhijiao_exports_from_device/*.gpx`

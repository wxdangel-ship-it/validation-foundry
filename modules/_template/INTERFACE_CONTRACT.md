# <module_id> - INTERFACE_CONTRACT

## 定位

- 本文件是 `<module_id>` 的稳定契约面
- 模块目标、上下文、构件关系与风险说明以 `architecture/*` 为准
- `README.md` 只承担操作者入口职责

## 1. 目标与范围

- 模块 ID：`<module_id>`
- 目标：`<补充模块目标>`
- 当前正式范围：
  - `<补充当前正式范围>`
- 当前非范围：
  - `<补充当前非范围>`

## 2. Inputs

### 2.1 必选输入

- `<input_name>`

### 2.2 可选输入

- `<optional_input_name>`

### 2.3 输入前提

- `<字段、格式、坐标系、前置状态等约束>`

## 3. Outputs

- `<output_artifact>`
- `<summary_or_metrics>`

说明：

- 输出目录、命名约定、关键字段、状态枚举和稳定诊断信息应在本节写清楚

## 4. EntryPoints

- `<补充官方入口；若当前未冻结，应明确写“未冻结”>`

## 5. Params

- 运行模式与路径：`<...>`
- 核心算法或规则参数：`<...>`
- 兼容参数：`<...>`

## 6. Acceptance

1. `<输出文件完整>`
2. `<关键输入输出约束可追溯>`
3. `<失败结果可诊断>`
4. `<关键业务规则不藏在 README 或临时脚本里>`

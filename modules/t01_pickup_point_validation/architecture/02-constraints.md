# 02 约束

## 状态

- 当前状态：`模块级约束说明`
- 来源依据：
  - `SPEC.md`
  - `INTERFACE_CONTRACT.md`

## 全局约束

- 不登录账号
- 当前输入输出坐标统一为 `GCJ-02`
- 不允许伪造坐标
- 每条输入必须有结果记录
- 滴滴链路优先，高德链路次选
- 不允许把未验证方法作为唯一主路径

## 工程约束

- 点击定位按钮后必须满足“至少 `3` 秒 + 稳定判据”才允许取点
- 若页面阻塞、应用限制或坐标不可信，必须输出 `FAIL/BLOCKED`
- 结果与证据目录必须保持一一对应

## 协作约束

- 当前阶段先完成 Phase 0 基线，不进入大规模实现
- 环境、链路、提取和批量执行要分线程推进并写回文档

# 002 - 真实设备回放后确认 app 行为阻塞

- 日期：`2026-03-27`
- 关联模块：`t01_pickup_point_validation`
- 关联阶段：`Phase 1 replay`、`Phase 2 replay`、`Phase 3 replay`

## 背景

在模拟器运行时阻塞后，项目切换到真实 ARM Android 设备验证，以区分“宿主运行时问题”和“目标 app 行为问题”。

## 本轮结论

- 真实设备 mock GPS 成功，说明输入侧不再是主阻塞
- 滴滴首页能够请求 `gps`，但在 `定位 + 稳定等待` 后仍落在北京 `茉藜园-东北门`，与 mock 上海点不一致
- 高德首页接收到 mock GPS，但打车业务入口在未登录约束下落入手机号登录层
- 因此 T01 当前从“模拟器阻塞”升级为“真实设备 app 行为阻塞”

## 影响

- 当前不能继续定义可信上车点坐标提取规则
- 当前不能启动 `10` 点 golden set 或批量执行
- 当前不能输出任何候选坐标

## 证据

- `outputs/_work/20260327_t01_real_device_blocked/`

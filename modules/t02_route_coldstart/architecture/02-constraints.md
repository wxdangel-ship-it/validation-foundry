# 02 约束

## 状态

- 当前状态：`模块级约束说明`
- 来源依据：
  - 当前任务书
  - `INTERFACE_CONTRACT.md`

## 全局约束

- 当前源平台仅允许 `2bulu` 与 `liuzhijiao`
- 不得把外部路线直接当作正式 Route 成品
- Route 草案包必须以几何为核心
- 必须保留原始证据
- 不得做协议逆向、未授权抓包或绕过导出限制

## 采集约束

- `2bulu` 只收类型标签明确包含 `越野车` 的路线
- `liuzhijiao` 只收由搜索 `越野` 命中且详情语义支持驾车越野的路线
- 必须通过详情页语义判断其主体是驾车越野
- `自驾` 不能单独作为入池条件
- 混合标签徒步 / 登山 / 步道路线必须排除

## 工程约束

- 不新增 repo root 级脚本
- 模块实现位于 `src/validation_foundry/modules/t02_route_coldstart/`
- 输出必须落到 `outputs/_work/`
- 默认不静默覆盖既有 run

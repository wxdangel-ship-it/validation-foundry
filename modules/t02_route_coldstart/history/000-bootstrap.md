# 000 - T02 Bootstrap

## 日期

- `2026-03-27`

## 背景

- 本轮任务显式批准启动 `t02_route_coldstart`
- 任务目标是基于两步路真实路线素材，至少整理出 `10` 个可进入人工审核池的越野 Route 草案包

## 当前冻结结论

1. 当前源平台允许：
   - `2bulu`
   - `liuzhijiao`
2. 当前采集门槛必须同时满足：
   - 命中平台级显式越野信号：
     - `2bulu`：`越野车` 标签
     - `liuzhijiao`：搜索 `越野` 命中且详情存在越野车辆信号
   - 详情页语义明确支持驾车越野判断
3. 混合标签徒步 / 登山 / 步道路线不得计入本轮目标
4. 产物结构按 Route 草案包、run 索引和 QA 汇总三层落盘
5. 当前实现入口冻结为：
   - `PYTHONPATH=src python3 -m validation_foundry.modules.t02_route_coldstart.pipeline`

## 与仓库既有规范的差异处理

- 仓库旧文档曾写“当前正式业务模块只有 T01”
- 本轮按用户任务显式批准，新增 T02，并同步补录生命周期与文档盘点

# T04_ACCEPTANCE_CHECKLIST

- [x] 所有静态图使用同一横屏平板画布，外框、留白和圆角一致
- [x] P2 页面主标题为 `越野路线详情`，模式标签只做徽标，不占主标题位
- [x] P2 路线名称只出现一次，概览区不再重复路线名称
- [x] P2 左侧内容块不显示 `XX卡` 标题，页面说明文案已明显减法
- [x] P2 底部固定操作区为 `导航至起点` 和 `进入越野地图页` 并排按钮
- [x] P2 地图默认以卫星图作为视觉基底，地图顶部只保留最少必要控制
- [x] P3 页面主标题为 `自由探索计划`，结构与减法原则和 P2 一致
- [x] P3 底部固定操作区为 `开始探索` 和 `进入越野地图页` 并排按钮
- [x] P3 探索边界只保留起点、锚点数、范围和超范围状态
- [x] P4 仅保留单层页面 chrome，地图默认以卫星图作为视觉基底
- [x] P4 页面内存在安全入口和结束 Trip 入口，地图背景保持可见
- [x] P4-overlay 为底部抽屉覆盖态，不遮挡地图核心内容
- [x] P5 含完成状态、总里程、总时长、回撤情况、一句话亮点和占位资产入口
- [x] Flow A 可点击跑通到 P5
- [x] Flow B 可点击跑通到 P5
- [x] Flow C 可点击跑通到 P5
- [x] Flow A / B / C 的 Storybook story 打开后可自动演示到预期终态
- [x] 下载三态可切换，自由探索 Ready Gate 可见
- [x] QA bundle 包含平板截图、流程截图和自动演示日志

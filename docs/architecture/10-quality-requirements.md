# 10 质量要求

## 1. 可理解

- 阅读入口清晰
- 阶段状态清晰
- 当前正式模块和非正式目录边界清晰

## 2. 可审计

- 每个阶段都有 gate 记录
- 每条样本都有结果与证据目录
- 成功与失败都可回查

## 3. 可复现

- 环境、应用、虚拟定位链路与取点规则必须可重放
- golden set 和批量执行都必须可重新运行

## 4. 可诊断

- 失败必须可归因
- `BLOCKED` 必须能区分环境阻塞、应用阻塞、提取阻塞

## 5. 边界明确

- 不把 README、AGENTS 或变更工件误写成唯一真相
- 不把阶段猜想冒充成已验证事实
